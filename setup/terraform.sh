#!/usr/bin/env bash

alias tf="now tf"

alias tf.apply="now terraform apply"
alias tf.destroy="now terraform destroy"
alias tf.get="now terraform get"
alias tf.graph="now terraform graph"
alias tf.init="now terraform init"
alias tf.output="now terraform output"
alias tf.plan="now terraform plan"
alias tf.push="now terraform push"
alias tf.refresh="now terraform refresh"
alias tf.remote="now terraform remote"
alias tf.show="now terraform show terraform.tfstate"
alias tf.taint="now terraform taint"
alias tf.version="now terraform version"
