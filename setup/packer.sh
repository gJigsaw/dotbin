#!/usr/bin/env bash

alias p="now packer"

alias p.build="now packer build"
alias p.fix="now packer fix"
alias p.inspect="now packer inspect"
alias p.push="now packer push"
alias p.validate="now packer validate"
alias p.version="now packer version"
