# Can you resolve and then ping www.google.com ?
function test_network () {
    if host google.com | grep -q timed
        then echo "DNS Failed";
    else ping -c 1 -W 1 google.com | grep -q bytes && echo "Network Fine." || echo "Network Offline.";
    fi
}
